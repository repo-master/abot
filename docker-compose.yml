version: '3.0'
services:
  abot_actions:
    image: abot/actions
    build:
      context: abot_actions
      dockerfile: ./abot-rasa/Dockerfile.actions
  frontend:
    image: abot/frontend
    build:
      context: frontend
      dockerfile: ./abot-frontend/Dockerfile.prod
  backend:
    image: abot/backend
    build: ./abot-backend
